{
  "name": "entregapro-monorepo",
  "private": true,
  "packageManager": "pnpm@11.5.0",
  "scripts": {
    "dev:api": "pnpm --filter api start:dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
